Each implementation is written in C, chosen not only because it keeps the code close to the reference specification while still being amenable to low-level optimizations, but also because all members of the group have some familiarity with the language. Using C also allows us to be more platform independent.

We adopted a unit test–driven approach to our AES‑128 implementations, where tests are written alongside the code and drive the structure of the design.
