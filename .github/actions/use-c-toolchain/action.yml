name: "Use C Toolchain"

description: "Setups the C toolchain for the project."

runs:
  using: "composite"
  steps:
    - name: "📦 Install dependencies"
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential cmake make
      shell: bash
